<% content_for(:title, t('.home')) %>
<div class="mx-auto max-w-screen-lg flex flex-col my-5 justify-center items-center bg-white rounded-xl border shadow py-5 md:py-10 md:px-5">
  <h1 class="font-bold text-xl md:text-2xl"><%= t('.title', user_name: @user.name) %></h1>
  <div class="w-full md:flex gap-10 my-5 p-3">
    <div class="flex flex-col h-full md:w-2/3">
      <p class="font-bold">最新の投稿</p>
      <%= posted_user? %>
    </div>
    <div class="flex flex-col gap-2 mt-6 md:w-1/3">
      <%= render 'my_page_menu' %>
    </div>
  </div>
</div>
